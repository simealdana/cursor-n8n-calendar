import { API_BASE_URL } from "@/config";

/**
 * Centralized API endpoint URLs
 * Add new endpoints here as needed
 */

export const API_ROOMS_URL = `${API_BASE_URL}/rooms`;
export const API_SLOTS_URL = `${API_BASE_URL}/slots`;
export const API_CREATE_RESERVATION_URL = `${API_BASE_URL}/slot`;

// Example for future endpoints:
// export const API_USERS_URL = `${API_BASE_URL}/users`;
// export const API_BOOKINGS_URL = `${API_BASE_URL}/bookings`;
